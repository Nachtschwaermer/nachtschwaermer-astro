# Expose Astro dependencies (e.g. sharp for image optimization) at the
# node_modules root. Previously set via .npmrc; pnpm 11 reads it from here.
shamefullyHoist: true
allowBuilds:
  esbuild: true
  sharp: true
overrides:
  js-yaml@<4.2.0: ^4.2.0
